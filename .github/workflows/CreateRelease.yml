name: Create release
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Git
        run: |
          git config user.name "TobyNa2001"
          git config user.email "nayikchun@gmail.com"

      - name: Create Release Tag
        run: |
          git tag v1.0.0
          git push origin v1.0.0
